//////////////////////////////////////////////////////////////////////////////////
//                                                                              //
//                 This is a native class documentation                         //
//                                                                              //
//                  This file is used only for DoxyGen                          //
//                                                                              //
//////////////////////////////////////////////////////////////////////////////////

namespace Dragengine.Scenery

pin Dragengine.Gui

/**
 * \brief Light.
 * 
 * This is a native class.
 */
class Light
	/** \name Constructors */
	/*@{*/
	/** \brief Create light. */
	func new()
	end
	/*@}*/
	
	
	
	/** \name Management */
	/*@{*/
	/** \brief Type. */
	func LightType getType()
		return null
	end
	
	/** \brief Set type. */
	func void setType(LightType type)
	end
	
	/**
	 * \brief Parent world or \em null if not added to a world.
	 * \version 1.29
	 */
	func World getParentWorld()
		return null
	end
	
	
	
	/** \brief Position. */
	func DVector getPosition()
		return null
	end
	
	/** \brief position. */
	func void setPosition(DVector position)
	end
	
	/** \brief Orientation. */
	func Quaternion getOrientation()
		return null
	end
	
	/** \brief Set orientation. */
	func void setOrientation(Quaternion orientation)
	end
	
	/** \brief Light is actived. */
	func bool getActivated()
		return false
	end
	
	/** \brief Set if light is activated. */
	func void setActivated(bool activated)
	end
	
	
	
	/** \brief Light color. */
	func Color getColor()
		return null
	end
	
	/** \brief Set light color. */
	func void setColor(Color color)
	end
	
	/** \brief Light intensity. */
	func float getIntensity()
		return 0.0
	end
	
	/** \brief Set light intensity. */
	func void setIntensity(float intensity)
	end
	
	/** \brief Range in meters. */
	func float getRange()
		return 0.0
	end
	
	/** \brief Set range in meters. */
	func void setRange(float range)
	end
	
	/** \brief Half intensity distance in meters. */
	func float getHalfIntensityDistance()
		return 0.0
	end
	
	/** \brief Set half intensity distance in meters. */
	func void setHalfIntensityDistance(float halfIntDist)
	end
	
	/** \brief Ratio of ambient light in relation to the total intensity. */
	func float getAmbientRatio()
		return 0.0
	end
	
	/** \brief Set ratio of ambient light in relation to the total intensity. */
	func void setAmbientRatio(float ratio)
	end
	
	/** \brief Light shape. */
	func ShapeList getShape()
		return null
	end
	
	/** \brief Set light shape. */
	func void setShape(ShapeList shape)
	end
	
	/** \brief Layer mask affecting lighting. */
	func LayerMask getLayerMask()
		return null
	end
	
	/** \brief Set layer mask affecting lighting. */
	func void setLayerMask(LayerMask layerMask)
	end
	/*@}*/
	
	
	
	/** \name Spot and projector */
	/*@{*/
	/** \brief Outer spot Angle. */
	func float getSpotAngle()
		return 0.0
	end
	
	/** \brief Set outer spot angle. */
	func void setSpotAngle(float angle)
	end
	
	/** \brief Spot ratio as height divided by width. */
	func float getSpotRatio()
		return 0.0
	end
	
	/** \brief Set spot ratio as height divided by width. */
	func void setSpotRatio(float ratio)
	end
	
	/** \brief Spot smoothness. */
	func float getSpotSmoothness()
		return 0.0
	end
	
	/** \brief Set spot smoothness. */
	func void setSpotSmoothness(float smoothness)
	end
	
	/** \brief Spot exponent. */
	func float getSpotExponent()
		return 0.0
	end
	
	/** \brief Set spot exponent. */
	func void setSpotExponent(float exponent)
	end
	/*@}*/
	
	
	
	/** \name Light source */
	/*@{*/
	/** \brief Light skin or \em NULL if not used. */
	func Skin getLightSkin()
		return null
	end
	
	/** \brief Set light skin or \em NULL if not used. */
	func void setLightSkin(Skin skin)
	end
	
	/** \brief Light canvas view or \em NULL if not used. */
	func CanvasView getLightCanvas()
		return null
	end
	
	/** \brief Set light canvas view or \em NULL if not used. */
	func void setLightCanvas(CanvasView canvas)
	end
	
	/** \brief Light dynamic skin or \em NULL if not used. */
	func DynamicSkin getDynamicSkin()
		return null
	end
	
	/** \brief Set light dynamic skin or \em NULL if not used. */
	func void setDynamicSkin(DynamicSkin dynamicSkin)
	end
	
	/** \brief Light texture coordinate transformation matrix. */
	func TexMatrix2 getTransform()
		return null
	end
	
	/** \brief Set light texture coordinate transformation matrix. */
	func void setTransform(TexMatrix2 matrix)
	end
	/*@}*/
	
	
	
	/** \name Shadow casting */
	/*@{*/
	/** \brief Light is casting shadows. */
	func bool getCastShadows()
		return false
	end
	
	/** \brief Set if light is casting shadows. */
	func void setCastShadows(bool castShadows)
	end
	
	/** \brief Layer mask affecting shadow casting. */
	func LayerMask getLayerMaskShadow()
		return null
	end
	
	/** \brief Set layer mask affecting shadow casting. */
	func void setLayerMaskShadow(LayerMask layerMask)
	end
	
	/** \brief Number of components to ignore for shadow casting. */
	func int getShadowIgnoreComponentCount()
		return 0
	end
	
	/**
	 * \brief Component to ignore for shadow casting at index.
	 * \throws EInvalidParam \em index is less than 0.
	 * \throws EInvalidParam \em index is greater or equal than getShadowIgnoreComponentCount().
	 */
	func Component getShadowIgnoreComponentAt(int index)
		return null
	end
	
	/** \brief Component to ignore for shadow casting is present. */
	func bool hasShadowIgnoreComponent(Component component)
		return false
	end
	
	/**
	 * \brief Add component to ignore for shadow casting.
	 * 
	 * Does nothing if \em component is present.
	 */
	func void addShadowIgnoreComponent(Component component)
	end
	
	/**
	 * \brief Remove component to ignore for shadow casting.
	 * Does nothing if \em component is absent.
	 */
	func void removeShadowIgnoreComponent(Component component)
	end
	
	/** \brief Remove all components to ignore for shadow casting. */
	func void removeAllShadowIgnoreComponents()
	end
	/*@}*/
	
	
	
	/** \name Hint */
	/*@{*/
	/** \brief Light importannce in the range from 0 (least important) to 100 (most important). */
	func int getHintLightImportance()
		return 0
	end
	
	/** \brief Sets light importannce in the range from 0 (least important) to 100 (most important). */
	func void setHintLightImportance(int type)
	end
	
	/** \brief Shadow important in the range from 0 (least important) to 100 (most important). */
	func int getHintShadowImportance()
		return 0
	end
	
	/** \brief Set shadow important in the range from 0 (least important) to 100 (most important). */
	func void setHintShadowImportance(int type)
	end
	
	/** \brief Movement hint. */
	func LightHintMovement getHintMovement()
		return null
	end
	
	/** \brief Set movement hint. */
	func void setHintMovement(LightHintMovement hint)
	end
	
	/** \brief Parameter change hint. */
	func LightHintParameter getHintParameters()
		return null
	end
	
	/** \brief Set parameter change hint. */
	func void setHintParameter(LightHintParameter hint)
	end
	
	/** \brief Light cage. */
	func ShapeList getCage()
		return null
	end
	
	/** \brief Set light cage. */
	func void setCage(ShapeList cage)
	end
	/*@}*/
end
